#name: Cypress Tests (Self-Hosted)
#
#on:
#  workflow_dispatch:  # Permite lanzarlo manualmente desde GitHub
#  push:               # También se ejecutará cuando hagas push
#
#jobs:
#  cypress-run:
#    runs-on: self-hosted   # 🔹 Usa tu runner local (con VPN)
#    permissions:
#      contents: read
#      actions: read
#      pull-requests: read
#
#    strategy:
#      fail-fast: false
#      matrix:
#        containers: [1]
#
#    steps:
#      - name: Checkout repository
#        uses: actions/checkout@v4
#
#      - name: Setup Node.js
#        uses: actions/setup-node@v4
#        with:
#          node-version: 20
#
#      - name: Install dependencies
#        run: npm ci
#
#      - name: Run Cypress tests (local runner)
#        uses: cypress-io/github-action@v6
#        with:
#          browser: chrome
#          headed: false
#          record: false    # Cambia a true si quieres reportar a Cypress Dashboard
#        env:
#          CYPRESS_RECORD_KEY: ${{ secrets.CYPRESS_RECORD_KEY }}
#          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
